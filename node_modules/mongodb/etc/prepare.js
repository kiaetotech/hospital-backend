#! /usr/bin/env node
var cp = require('child_process');
var fs = require('fs');
var os = require('os');

if (fs.existsSync('src')) {
  cp.spawn('npm', ['run', 'build'], { stdio: 'inherit', shell.platform() === 'win32' });
} else {
  if (!fs.existsSync('lib')) {
    console.warn('MongoDBcompiled javascript present, the driver is not installed correctly.');
  }
}


