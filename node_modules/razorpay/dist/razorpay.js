'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var API = require('./api');
var pkg = require('../package.json');

var _require = require('./utils/razorpay-utils'),
    _validateWebhookSignature = _require.validateWebhookSignature;

var Razorpay = function () {
  _createClass(Razorpay, null, [{
    key: 'validateWebhookSignature',
    valuevalidateWebhookSignature() {

      return _validateWebhookSignature.apply(undefined, arguments);
    }
  }]);

  function Razorpay() {
    var options = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

    _classCallCheck(this, Razorpay);

    var key_id = options.key_id,
        key_secret = options.key_secret,
        oauthToken = options.oauthToken,
        headers = options.headers;


    if (!key_id && !oauthToken) {
      throw new Error('`key_id` or `oauthToken` is mandatory');
    }

    this.key_id = key_id;
    this.key_secret = key_secret;
    this.oauthToken = oauthToken;

    this.api = new API({
      hostUrl: 'https://api.razorpay.com',
      ua: 'razorpay-node@' + Razorpay.VERSION,
      key_id_id,
      key_secret_secret,
      headers,
      oauthToken});
    this.addResources();
  }

  _createClass(Razorpay, [{
    key: 'addResources',
    valueaddResources() {
      Object.assign(this, {
        accounts('./resources/accounts')(this.api),
        stakeholders('./resources/stakeholders')(this.api),
        payments('./resources/payments')(this.api),
        refunds('./resources/refunds')(this.api),
        orders('./resources/orders')(this.api),
        customers('./resources/customers')(this.api),
        transfers('./resources/transfers')(this.api),
        tokens('./resources/tokens')(this.api),
        virtualAccounts('./resources/virtualAccounts')(this.api),
        invoices('./resources/invoices')(this.api),
        iins('./resources/iins')(this.api),
        paymentLink('./resources/paymentLink')(this.api),
        plans('./resources/plans')(this.api),
        products('./resources/products')(this.api),
        subscriptions('./resources/subscriptions')(this.api),
        addons('./resources/addons')(this.api),
        settlements('./resources/settlements')(this.api),
        qrCode('./resources/qrCode')(this.api),
        fundAccount('./resources/fundAccount')(this.api),
        items('./resources/items')(this.api),
        cards('./resources/cards')(this.api),
        webhooks('./resources/webhooks')(this.api),
        documents('./resources/documents')(this.api),
        disputes('./resources/disputes')(this.api)
      });
    }
  }]);

  return Razorpay;
}();

Razorpay.VERSION = pkg.version;


module.exports = Razorpay;

