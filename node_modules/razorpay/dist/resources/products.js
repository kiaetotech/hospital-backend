'use strict';

module.exports = function (api) {

    var BASE_URL = "/accounts";

    return {
        requestProductConfigurationrequestProductConfiguration(accountId, params, callback) {
            return api.post({
                version: 'v2',
                url_URL + '/' + accountId + '/products',
                data}, callback);
        },
        editedit(accountId, productId, params, callback) {
            return api.patch({
                version: 'v2',
                url_URL + '/' + accountId + '/products/' + productId,
                data}, callback);
        },
        fetchfetch(accountId, productId, callback) {
            return api.get({
                version: 'v2',
                url_URL + '/' + accountId + '/products/' + productId
            }, callback);
        },
        fetchTncfetchTnc(productName, callback) {
            return api.get({
                version: 'v2',
                url: '/products/' + productName + '/tnc'
            }, callback);
        }
    };
};

