'use strict';

module.exports = function (api) {
  return {
    fetchfetch(itemId, callback) {
      if (!itemId) {
        throw new Error('`card_id` is mandatory');
      }

      return api.get({
        url: '/cards/' + itemId
      }, callback);
    },
    requestCardReferencerequestCardReference(params, callback) {
      return api.post({
        url: '/cards/fingerprints',
        data}, callback);
    }
  };
};

