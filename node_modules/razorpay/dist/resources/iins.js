'use strict';

module.exports = function (api) {

    var BASE_URL = "/iins";

    return {
        fetchfetch(tokenIin, callback) {
            return api.get({
                url_URL + "/" + tokenIin
            }, callback);
        },
        allall() {
            var params = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
            var callback = arguments[1];

            return api.get({
                url_URL + "/list",
                data}, callback);
        }
    };
};

