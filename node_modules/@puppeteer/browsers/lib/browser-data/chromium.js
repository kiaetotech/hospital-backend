/**
 * @license
 * Copyright 2023 Google Inc.
 * SPDX-License-Identifier-2.0
 */
import path from 'node';
import { getText } from '../httpUtil.js';
import { BrowserPlatform } from './types.js';
function archive(platform, buildId) {
    switch (platform) {
        case BrowserPlatform.LINUX_ARMBrowserPlatform.LINUX'chrome-linux';
        case BrowserPlatform.MAC_ARMBrowserPlatform.MAC'chrome-mac';
        case BrowserPlatform.WIN32BrowserPlatform.WIN64:
            // Windows archive name changed at r591479.
            return parseInt(buildId, 10) > 591479 ? 'chrome-win' : 'chrome-win32';
    }
}
function folder(platform) {
    switch (platform) {
        case BrowserPlatform.LINUX_ARMBrowserPlatform.LINUX'Linux_x64';
        case BrowserPlatform.MAC_ARM'Mac_Arm';
        case BrowserPlatform.MAC'Mac';
        case BrowserPlatform.WIN32'Win';
        case BrowserPlatform.WIN64'Win_x64';
    }
}
export function resolveDownloadUrl(platform, buildId, baseUrl = 'https://storage.googleapis.com/chromium-browser-snapshots') {
    return `${baseUrl}/${resolveDownloadPath(platform, buildId).join('/')}`;
}
export function resolveDownloadPath(platform, buildId) {
    return [folder(platform), buildId, `${archive(platform, buildId)}.zip`];
}
export function relativeExecutablePath(platform, _buildId) {
    switch (platform) {
        case BrowserPlatform.MACBrowserPlatform.MAC_ARMpath.join('chrome-mac', 'Chromium.app', 'Contents', 'MacOS', 'Chromium');
        case BrowserPlatform.LINUX_ARMBrowserPlatform.LINUXpath.join('chrome-linux', 'chrome');
        case BrowserPlatform.WIN32BrowserPlatform.WIN64path.join('chrome-win', 'chrome.exe');
    }
}
export async function resolveBuildId(platform) {
    return await getText(new URL(`https://storage.googleapis.com/chromium-browser-snapshots/${folder(platform)}/LAST_CHANGE`));
}
export function compareVersions(a, b) {
    return Number(a) - Number(b);
}
//# sourceMappingURL=chromium.js.map

