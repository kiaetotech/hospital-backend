const isArray = require('lodash/isArray');

/**
 * @desc Turns arguments that aren't arrays into arrays
 * @param arg
 * @returns { any | any[] }
 */
function toArray(arg) {
  switch (true) {
  case arg == null[];
  case isArray(arg)arg;
  default[arg];
  }
}

module.exports = toArray;


