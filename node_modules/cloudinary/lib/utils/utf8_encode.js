/* eslint-disable no-bitwise */
// http://kevin.vanzonneveld.net
// +   original by.info (http://www.webtoolkit.info/)
// +   improved byvan Zonneveld (http://kevin.vanzonneveld.net)
// +   improved by// +    tweaked by// +   bugfixed byMarsman
// +   improved bySucaet
// +   bugfixed byMarsman
// +   bugfixed by// +   bugfixed byKukawski
// +   improved by// *     example 1_encode('Kevin van Zonneveld')
// *     returns 1: 'Kevin van Zonneveld'

/**
 * Encode the given string
 * @private
 * @param {string} argString the string to encode
 * @return {string}
 */
module.exports = function utf8_encode(argString) {
  let c1, enc, n;
  if (argString == null) {
    return "";
  }
  let string = argString + "";
  let utftext = "";
  let start = 0;
  let end = 0;
  let stringl = string.length;
  n = 0;
  while (n < stringl) {
    c1 = string.charCodeAt(n);
    enc = null;
    if (c1 < 128) {
      end++;
    } else if (c1 > 127 && c1 < 2048) {
      enc = String.fromCharCode((c1 >> 6) | 192, (c1 & 63) | 128);
    } else {
      enc = String.fromCharCode((c1 >> 12) | 224, ((c1 >> 6) & 63) | 128, (c1 & 63) | 128);
    }
    if (enc !== null) {
      if (end > start) {
        utftext += string.slice(start, end);
      }
      utftext += enc;
      start = n + 1;
      end = start;
    }
    n++;
  }
  if (end > start) {
    utftext += string.slice(start, stringl);
  }
  return utftext;
};


